"""Top-level `finetune/` package — synthetic clinical-encounter datagen and
local LLM fine-tuning helpers. Deliberately kept OUTSIDE the `carescribe/`
app package so it is never frozen into the desktop build."""
