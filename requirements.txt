# CareScribe — pinned dependencies (Python 3.11+)
#
# Everything here runs locally on the CPU. No cloud SDKs, no telemetry clients,
# no API keys. The de-identification stage makes no network calls at all.

streamlit==1.41.1

# Backs st.data_editor's identifier table (streamlit pulls it in anyway;
# pinned here because app.py imports it directly).
pandas==2.2.3

# Document ingestion
pdfplumber==0.11.5
pypdf==5.1.0
python-docx==1.1.2

# ---------------------------------------------------------------------------
# De-identification layer 2 — NER over free text, CPU-only.
#
# The spaCy model is not a pip package; install it after these:
#     python -m spacy download en_core_web_lg
# The pipeline falls back to en_core_web_sm automatically, and to the
# structured regex layer alone if neither model is present.
# ---------------------------------------------------------------------------
presidio-analyzer==2.2.364
presidio-anonymizer==2.2.364
spacy==3.8.14

# Tests
pytest==9.1.1

# ---------------------------------------------------------------------------
# OPTIONAL — de-identification layer 3.
#
# GLiNER is a small CPU NER model that adds recall on hard names and orgs in
# free text. It pulls in torch (~2GB), so it is deliberately NOT installed by
# default; the import is guarded and the app runs identically without it.
#
#     pip install gliner
# ---------------------------------------------------------------------------
# gliner==0.2.21

# ---------------------------------------------------------------------------
# NOT USED IN THIS STAGE.
#
# carescribe/core/ollama_client.py is retained for the eventual generation
# stage but is imported by nothing. Leaving `ollama` uninstalled is the
# stronger guarantee that nothing in this stage talks to a server.
# ---------------------------------------------------------------------------
# ollama==0.4.7
