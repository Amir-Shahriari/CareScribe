# CareScribe — pinned dependencies (Python 3.11+)
# Everything here runs locally. No cloud SDKs, no telemetry clients, no API keys.

streamlit==1.41.1
ollama==0.4.7

# Backs st.data_editor's identifier table (streamlit pulls it in anyway;
# pinned here because app.py imports it directly).
pandas==2.2.3

# Document ingestion
pdfplumber==0.11.5
pypdf==5.1.0
python-docx==1.1.2

# Phase 2 (optional, NOT installed by default — see README):
# presidio-analyzer==2.2.355
# spacy==3.7.5
